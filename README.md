CONFIG_PATH=/etc/secrets/config.yaml
DEPLOY=cloud
MANAGEMENT_STATIC_PATH=/CLIProxyAPI/static


### Bổ sung những cái này vào Environment Variables ở trên render
### Copy file config.example.yaml vào trong secret file trên render và rename là confg.yamlyaml

truy cập https://your-app.com/management.html với password là secret-key ở trong config.yaml để có thể đăng nhập 
